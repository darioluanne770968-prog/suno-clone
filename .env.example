# PiAPI Configuration (Recommended)
# Get your API key from https://app.piapi.ai
# Sign up with GitHub, then get API key from dashboard
PIAPI_KEY=your_piapi_key_here

# Set to 'true' to use mock data instead of real API (for development)
NEXT_PUBLIC_USE_MOCK=true

# Alternative: SunoAPI.org (if PiAPI not available)
# SUNO_API_KEY=your_suno_api_key_here
